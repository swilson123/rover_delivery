// Minimal placeholder for pixhawk_drone module.
// Implement drone-specific helper functions here. This prevents require() from returning an empty module.

module.exports = {
	// Initialize the drone-specific helpers. Should be replaced with real implementation.
	init: function (rover) {
		// no-op for now
		return true;
	}
};
